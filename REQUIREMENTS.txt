# Don't forget to download Python first(We are running this program with Python): https://www.python.org/downloads/
# Detailed set up (step by step guide): https://youtu.be/w_AGre6-9TM?si=gE5xc4_aKhy4DwnA

# SETUP INSTRUCTIONS:
# 1. Install Python dependencies (run command below)
# 2. Install Ollama: https://ollama.ai/download
# 3. Pull phi3:mini model: ollama pull phi3:mini
# 4. Start Ollama: ollama serve
# 5. Add SerpAPI key to config/api_keys.json

# Required Python packages:
pip install sounddevice
pip install soundfile
pip install numpy
pip install openai-whisper  # For Whisper STT (small model)
pip install websockets      # For Web Speech API
pip install edge-tts
pip install python-dotenv
pip install google-search-results
pip install Pillow
pip install requests
pip install pyautogui
pip install pywebview
pip install psutil           # For system monitoring
pip install mss              # For screen capture
pip install Pillow           # For image processing (already listed above, ensure installed)
